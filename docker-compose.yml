version: '3.8'

services:
  context7-mcp-server:
    image: tolak/context7-mcp-server:v0.1.0
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
